node_modules/
dist/
.DS_Store
*.log

# Environment variables - NEVER COMMIT
.env
.env.local
.env.production.local

# Private config in root - NEVER COMMIT (use for local dev only)
config.js

# Note: public/config.js IS committed as a default template
# The prebuild script overwrites it with real values from env vars
